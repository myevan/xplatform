XPlatform
=========

Crossplatform Ports

Usage
-----

    $ cd ports
    $ ./manage.py prepare $PACKAGE_DIR
    $ cd ../sources/$PACKAGE_DIR
    $ mkdir build
    $ cd build
    $ cmake ..
    $ make install
